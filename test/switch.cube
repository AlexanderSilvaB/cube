var n = num(input("Number: "));

switch (n) 
{
    case 1:
        println("one");
    case 2:
        println("two");
    case 3:
        println("three");
    default:
        println("default ({})".format(n));
}

print("after");