import math as default;

var dpi = deg(pi);
var rpi = rad(dpi);

println('deg({}) = {}\nrad({}) = {}'.format(pi, dpi, dpi, rpi));

var x = -7;
var y = abs(x);
println('abs({}) = {}'.format(x, y));

seed();
x = int(random() * 1000);
y = int(random() * 1000);
println('min({}, {}) = {}\nmax({}, {}) = {}'.format(x, y, min(x, y), x, y, max(x, y)));

x = 57;
println('sqrt({}) = {}'.format(x, sqrt(x)));

println('sin({}) = {}'.format(pi, sin(pi)));
println('cos({}) = {}'.format(pi, cos(pi)));