import math as m

func string.len()
{
    return len(this)
}

func number.sig()
{
    if(this > 0)
        return 1
    return -1
}

str = "ABC"
println(str,".len() = ", str.len())

println(5, ".sig() = ", 5.sig())
println(-5, ".sig() = ", -5.sig())
