native libs/libgame
{
    void title(str);
    void size(num, num);
    void rgb(num, num, num);
    void rgba(num, num, num, num);
    void alpha(num, num);
    bool full(bool);
    void update();
    void clear();
    void translate(num, num, num);
    void move(num, num, num);
    void rotate(num, num);
    void rotation(num, num);
    void flip(num, bool, bool);
    void draw(num);
}